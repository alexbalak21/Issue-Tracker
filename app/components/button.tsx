"use client"
import React from "react"

function UserCard() {
    return (
        <div>
            <button onClick={() => console.log("Cicked Button")}>Click</button>
        </div>
    )
}

export default UserCard
